{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
            <tr>
                <th>Contenu</th>
                <td>{{ tag.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tag_index') }}">Retour liste</a>

    <a href="{{ path('app_tag_edit', {'id': tag.id}) }}">modifier</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
